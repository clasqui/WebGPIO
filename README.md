WebGPIO
=======

WebGPIO is a ready to use web interface to control your gpio pins in your raspberry pi.  It's created with the latest technologies and only requires node.js in your raspberry.

#Install
To install the server just run `npm install -g webgpio` to install the package generally in your Raspberry Pi.
